version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - pip3 install flask pytest

  build:
    commands:
      - echo "Build successful"

artifacts:
  files:
    - '**/*'
  discard-paths: no